# Dev Log — 2026-06-06 09:00

## Summary (human review)
Fixed all 7 issues identified in the Playwright test run. System now handles bad login gracefully, HOD can view at-risk students, counselor cases auto-populate on academic standing decline, academic standing correctly reflects worst-course weighted average, teacher posting history page is live, and DPE officers can send circulars from the dashboard UI.

## Session Details
- **Date**: 2026-06-06
- **Token estimate**: ~120k
- **Context**: Continued from previous session; summary provided via JSONL transcript

## Completed Work

### Fix 1 — Login Error Display (LoginPage.tsx)
- `doLogin()` now throws when `data.success === false`, so `message.error(t('auth.loginError'))` in `onFinish` / `onQuickLogin` actually fires
- Merged remote's improved demo accounts panel (tiered Storyline/Multi-authority/Additional structure) on top of this fix

### Fix 2 — HOD At-Risk Access (ai.ts)
- Added `'hod'` and `'counselor'` to `requireRole` for three endpoints:
  - `GET /ai/risk/students`
  - `GET /ai/risk-report/:gradeLevel`
  - `GET /ai/risk-grades`
- HOD dashboard now populates Year 7 at-risk students (Ahmad = HIGH risk, score 80)

### Fix 3 — Academic Standing Detection (ai.ts)
- `recalcStudentRisk` now computes per-course **weighted averages** (using `GradeItem.weight`) and uses the **minimum (worst) course avg** as input to `updateAcademicStanding`
- Ahmad's MATH701: Week1=77(w0.1), Week2=72(w0.1), Week3=68(w0.1), Week4=65(w0.1), Final=42(w0.4) → weighted avg = 56.25% → ACADEMIC_WATCH ✓
- Previously used flat average of all grades across all courses (68%) → GOOD_STANDING (wrong)

### Fix 4 — Counselor Case Auto-Population (ai.ts)
- `updateAcademicStanding` now auto-creates a `CounselorCase` with reason `AUTO_STANDING_DECLINE` when standing worsens (any direction: GOOD→WATCH, GOOD→PROBATION, WATCH→PROBATION)
- Existing open cases are not duplicated
- Verified: Ahmad's standing recalc created case status=OPEN, reason=AUTO_STANDING_DECLINE

### Fix 5 — Teacher Postings History Page (new)
- Created `pc/src/pages/ems/PostingsPage.tsx`: table of all teacher career/posting records, filterable by teacher, with add-record modal (admin/manager/principal)
- Added `GET /postings` backend endpoint with teacher name included (via Prisma include)
- Wired route `/ems/postings` and sidebar entry under EMS → "Postings History"

### Fix 6 — DPE Send Circular UI (PrivateEdDashboardPage.tsx)
- Added "Send Circular" button in header
- Modal with fields: circularNumber, title, bodyMarkdown, effectiveDate, acknowledgementDueDate, targetScope (ALL_PRIVATE / BY_DISTRICT / SPECIFIC), targetDistrict (conditional)
- Tested: DPE/CIR/2026/06 created targeting 5 private schools ✓

### Additional (admissions.ts)
- Minor: echo `gradeApplied` back in eligibility check response

## Files Changed
- `backend/src/routes/ai.ts` — role fixes, per-course weighted avg, counselor auto-case
- `backend/src/routes/postings.ts` — new GET / endpoint
- `backend/src/routes/admissions.ts` — echo gradeApplied
- `pc/src/pages/auth/LoginPage.tsx` — doLogin error throw + merge remote panel
- `pc/src/pages/ems/PostingsPage.tsx` — new file
- `pc/src/pages/privateEd/PrivateEdDashboardPage.tsx` — Send Circular button + modal
- `pc/src/router/routes.tsx` — PostingsPage route
- `pc/src/layouts/Sidebar.tsx` — Postings History nav entry

## Verification
- TypeScript: both frontend + backend `tsc --noEmit` → 0 errors
- API tests: HOD risk-grades 200 ✓, Ahmad standing=ACADEMIC_WATCH ✓, counselor case AUTO_STANDING_DECLINE ✓, postings 8 records ✓, DPE circular targets=5 ✓
