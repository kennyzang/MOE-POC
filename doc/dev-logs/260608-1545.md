# Dev Log 260608-1545

## Summary (人工阅读)

第三轮移动端 QA 修复：悬浮按钮可拖拽、Demo 账号精简、评估数据去重复。

## 详细记录

**会话时间**: 2026-06-08 ~15:45  
**Token 估算**: ~20k

### 完成内容

#### 1. 浮动 AI 按钮支持拖拽 (`mobile/src/components/ChatDrawer.tsx`)
- 新增 `fabPos` state 记录按钮当前位置（bottom/right）
- 通过 `onTouchStart` / `onTouchMove` / `onTouchEnd` 实现拖拽定位
- 使用 non-passive `touchmove` 监听器防止拖拽时页面滚动（`{ passive: false }`）
- tap（未移动）仍触发打开聊天，drag 则移动按钮
- CSS 改为 `position: fixed` + inline style 控制位置，`cursor: grab`

#### 2. Demo 账号精简为 3 个 (`mobile/src/pages/auth/LoginPage.tsx`)
- 只保留：Form Teacher（teacher01）、Year 7 Student（student001）、Parent（parent01）
- 删除 Admin / Principal / HOD（移动端不涉及这些角色）
- 修正 parent 账号：`parent.siti / Demo@2026` → `parent01 / parent123`（与 CLAUDE.md 一致）
- 3 个账号可完全展示，不再出现省略号

#### 3. 即将到来评估数据修复 (`backend/prisma/seed.ts` + `backend/src/routes/dashboard.ts`)
- **seed.ts**：将 5 门课程的评估项从 "Quiz 1 / Midterm Exam / Assignment 1 / Final Exam"（全课相同）改为课程专属名称：
  - 数学：Chapter 3 Algebra Quiz / Statistics Worksheet
  - 英语：Reading Comprehension Quiz / Descriptive Essay
  - 科学：Forces & Motion Quiz / Lab Report: Simple Machines
  - MIB：MIB Values Assessment / Reflection Essay
  - ICT：Digital Literacy Quiz / Spreadsheet Mini-Project
- 各项均设置了错开的未来截止日期（Quiz: +7~11天, Assignment: +14~18天, Midterm: +22天, Final: 2026-07-10）
- adam/nurul 仍有对应成绩（不计入"即将到来"列表）
- **dashboard.ts**：`upcomingItems` 查询增加 `dueDate: { gte: new Date() }` 过滤，并 `take: 10` 限制数量，避免显示空日期或已过期项目

### 变更文件
- `mobile/src/components/ChatDrawer.tsx`
- `mobile/src/pages/auth/LoginPage.tsx`
- `mobile/src/styles/global.css`
- `backend/src/routes/dashboard.ts`
- `backend/prisma/seed.ts`

### 验证
- `mobile npm run build` ✓
- `backend npx tsc --noEmit` ✓
- DB 重置后种子数据验证：10 条即将到来评估均有不同名称、正确截止日期
- 已推送：`git push github master` ✓
