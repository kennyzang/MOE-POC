---
session: 2026-06-10 14:00
tokens: ~80k
---

# 演示日 Demo Fixes — 招生账号/通知链接/跨校过滤

## 摘要（人工阅读）

演示日当天最后一批修复，全部推送至双端。主要解决招生账号演示快捷栏缺失、通知点击无跳转、招生列表顺序、跨校申请可见性四个问题，并重置数据库（3456学生+12招生记录）。

---

## 完成工作

### 1. Demo Accounts 修正（LoginPage.tsx）
- 将 `admission/admission123` 更正为 `admissions/Demo@2026`（与演示剧本 ACT 2 一致）
- 按演示剧本 ACT 顺序重排：admissions → teacher01 → parent.siti → drsiti → admin → hod01 → student001 → farah → principal → sysadmin

### 2. 招生通知增加 link 字段（registration.ts + admissions.ts）
- 所有 `sendMany` 调用添加 `link: '/sis/admissions'`
- 修复前：通知被点击仅 markAsRead，蓝色高亮消失看起来像"消失"
- 修复后：点击通知直接导航到 `/sis/admissions`

### 3. 招生列表排序（admissions.ts）
- `orderBy: [{ submittedAt: 'desc' }, { createdAt: 'desc' }]`（之前按 createdAt）

### 4. 跨校申请可见性修复（admissions.ts GET /）
- 招生角色（`admissions`）完全绕过学校过滤，可查看所有学校的申请
- 之前：仅查到 `schoolId === smhk.id` 的申请，其他学校申请不可见

### 5. 跨校通知回退（registration.ts POST /submit）
- 无学校专属招生人员时，回退通知全部招生人员
- 修复 APP-2026-0013（ISB 学校申请）无通知的问题

### 6. 数据库重置
- `npx prisma db push --force-reset && npx tsx prisma/seed.ts`
- 验证：3456 学生，12 招生记录，正常

---

## 修改文件

| 文件 | 变更 |
|------|------|
| `pc/src/pages/auth/LoginPage.tsx` | Demo Accounts 顺序 + admissions 账号修正 |
| `backend/src/routes/admissions.ts` | 排序 + 通知 link + admissions 角色绕过学校过滤 |
| `backend/src/routes/registration.ts` | 通知 link + 跨校回退通知 |

## 提交记录

- `036fb11` fix(admissions): show all applications to admissions role, fix cross-school notifications
- `0bdc5cc` fix(login): reorder Demo Accounts to match oral demo script
- `e0ce652` fix(admissions): add admission account to demo panel, fix notification link, sort by submittedAt

## 推送状态

- `github master` ✓ 推送成功
- `origin master` ✓ 推送成功
