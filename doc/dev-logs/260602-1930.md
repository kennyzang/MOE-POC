---
session: 260602-1930
date: 2026-06-02 19:30
tokens: ~35k
---

# 260602-1930 — Modules 5, 6, 7: Inventory · Non-Teaching Staff · School Profile

## 摘要（人工速览）

连续完成三个模块，全部 TypeScript 零错误，代码已推送。

- **Module 5** — Asset Inventory：12 端点（分类管理 + 资产 CRUD + 维护日志），前端完整页面
- **Module 6** — Non-Teaching Staff：扩展 /teachers 支持 staffType 过滤，TeacherDirectory 新增 Segmented 分组，TeacherDetail 按 staffType 隐藏 CPD/Performance/Courses 标签
- **Module 7** — School Profile：PUT /schools/:id + GET /schools/:id/stats + SchoolProfilePage

## Module 5 — Asset Inventory

### 5A 后端 `backend/src/routes/inventory.ts`

分类（4 端点）：GET（含 assetCount）/ POST（P2002 重名守卫）/ PUT / DELETE（409 含资产数）

资产（5 端点）：GET list（schoolId + 多字段过滤）/ GET :id（含 maintenanceLogs）/ POST（assetTag 自动生成：`CAT-YEAR-NNNN`，P2002 守卫）/ PUT / DELETE（级联维护日志）

维护（2 端点）：POST（Disposal → 自动设置 condition=Condemned）/ DELETE

### 5B 前端 `pc/src/pages/sms/InventoryPage.tsx`

- 页头：Manage Categories + Register Asset 按钮
- Filter bar：搜索 + 分类 Select + 状态 Select + 位置输入
- 资产表格：编号/名称/分类/位置/状态（彩色 Tag）/购入日期/价值
- 资产详情 Drawer：统计行 + 快速状态 Select + 内联添加维护表单（Disposal 有 warning banner + Popconfirm）+ 维护表格
- 分类管理 Modal：List 内联编辑 + 底部添加 + 删除禁用（有资产时 tooltip 提示）

## Module 6 — Non-Teaching Staff

### 6A 后端 `backend/src/routes/teachers.ts`

新增 `staffType` 查询参数，支持逗号分隔多值（`ADMINISTRATIVE,SUPPORT,SECURITY`），转为 Prisma `{ in: [...] }` 过滤。

### 6B 前端

**TeacherDirectoryPage.tsx**
- 新增 `Segmented` 控件：All / Teaching Staff / Admin & Support
- Teaching Staff → `?staffType=TEACHING`；Admin & Support → `?staffType=ADMINISTRATIVE,SUPPORT,SECURITY`
- Admin & Support 模式：隐藏 Subjects 和 CPD Hours 列；显示 Role/Function 列（映射 staffType 到标签）

**TeacherDetailPage.tsx**
- `isTeaching = !teacher.staffType || teacher.staffType === 'TEACHING'`
- 非教学人员隐藏 Courses / Performance / CPD 标签，仅显示 Overview / Certifications / Leave

## Module 7 — School Profile

### 7A 后端

- 新增 `motto String?` 字段到 School 模型，`prisma db push` 同步
- `PUT /schools/:id`：更新 name/address/phone/principal/motto/schoolType（admin/manager 权限）
- `GET /schools/:id/stats`：返回 studentCount/teachingStaffCount/nonTeachingStaffCount/facilityCount

### 7C 前端 `pc/src/pages/sms/SchoolProfilePage.tsx`

- 页头：学校名 + authority Tag + code Tag
- 4 张统计卡：学生数 / 教学人员 / 支援人员 / 设施数
- School Info Card：查看模式（Descriptions）↔ 编辑模式（Form），Edit/Save/Cancel 切换
- Facilities Card：表格显示所有设施，每行有 Book 快速链接到 /sms/resources

## Sidebar 变更

SMS 分组新增：
- Library（Library 图标，admin/manager/principal/teacher/finance）
- Inventory（Package 图标，admin/manager）
- School Profile（School 图标，admin/manager/principal）

Finance Portal 新增：Library

## 修改文件清单

| 文件 | 变更 |
|------|------|
| `backend/prisma/schema.prisma` | School 模型新增 motto 字段 |
| `backend/src/routes/inventory.ts` | 新建，12 端点 |
| `backend/src/routes/schools.ts` | 新增 PUT /:id + GET /:id/stats |
| `backend/src/routes/teachers.ts` | GET / 新增 staffType 过滤 |
| `backend/src/index.ts` | 注册 /api/v1/inventory |
| `pc/src/pages/sms/InventoryPage.tsx` | 新建 |
| `pc/src/pages/sms/SchoolProfilePage.tsx` | 新建 |
| `pc/src/pages/ems/TeacherDirectoryPage.tsx` | Segmented + 条件列 |
| `pc/src/pages/ems/TeacherDetailPage.tsx` | staffType 条件 Tab |
| `pc/src/router/routes.tsx` | 新增 inventory + school-profile 路由 |
| `pc/src/layouts/Sidebar.tsx` | 新增 Inventory + School Profile + Library(finance) |
| `pc/src/locales/en/zh/ms.ts` | schoolProfile.* + inventory.* + ems.allStaff/teachingStaff/adminAndSupport/roleFunction + nav.smsInventory/smsSchoolProfile |

## Commits

- `c5e8075` feat(sms): Module 5 — Asset Inventory
- (M6 + M7 会在本次 log 同 commit)
