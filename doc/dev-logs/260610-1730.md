# Dev Log: 2026-06-10 17:30

## 第二/三轮调整：演示环境准备

### 1. 账号系统统一
- 所有演示账号密码统一为 `Demo@2026`（seed.ts, PC/mobile 登录页）
- 演示账号列表对齐 PC ↔ 移动端
- teacher01 排第一位（PC + mobile）

### 2. 教师点名修复
- 后端 `/roll-call/today` 返回 `records`（studentId + status）
- 前端打开弹窗时预填上次保存的状态
- 可编辑已有记录，也可新增

### 3. 消息功能完善
- 后端新增 `DELETE /messages/threads/:id`
- PC 端：家长 / 教师 / communications panel 三个消息入口均添加删除按钮
- 移动端：家长消息列表添加删除图标（点选确认）
- 教师移动端 bottom tab 添加 Messages 入口

### 4. 联系人排序
- 后端 `GET /parent/children`：Ahmad / Hafiz 排在最前
- 后端 `GET /messages/teacher-contacts`：家长优先于学生，演示家长置顶
- 后端 `GET /parent/meetings/teachers`：teacher01（Ms. Aminah）排最前

### 5. 移动端改进
- 消息详情页滚动条修复（absolute 布局脱离 page-content padding）
- 家长消息页 + 按钮移左侧（避免与 AI 聊天按钮重叠）
- 家长/教师新消息弹窗：Selector 改为可搜索下拉弹窗

### 6. 注册 & 状态查询
- 注册门户上传文件真实写入磁盘（base64 → file）
- 状态查询改为单字段（Application ID / Student ID 均可），去掉 Parent IC
- 已录取时显示 Username / Password 可直接复制
- 新增 `tempPassword` 字段存储明文临时密码
- 登录页添加注册/查询入口链接

### 7. 演示脚本更新
- 所有旧密码更新为 `Demo@2026`
- 移动端端口从 5173 更新为 3001
- 添加密码统一提示

### 文件改动（15 files, +509/-134）
- `backend/prisma/schema.prisma` — 新增 Admission.tempPassword 字段
- `backend/prisma/seed.ts` — 统一密码
- `backend/src/routes/messages.ts` — 删除端点 + 联系人排序
- `backend/src/routes/parent.ts` — 联系人排序
- `backend/src/routes/registration.ts` — 单字段查询 + tempPassword 返回
- `backend/src/services/enrollmentService.ts` — 存储 tempPassword
- `docs/demo-oral-script.md` — 密码/端口更新
- 前端文件 × 7 — 各模块 UI 改进
