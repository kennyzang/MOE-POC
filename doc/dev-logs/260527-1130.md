# Dev Log 260527-1130

## Summary (for human review)
第二轮验收修复：全面过了 260527.md 的所有未解决问题，涉及登录体验、导航高亮、Principal 权限、教师/学生仪表盘内容、管理员图表视觉。

## Session Info
- Date: 2026-05-27
- Time: ~11:30
- Token estimate: ~25k
- Commits: 1 (`b4993f1`)

## Work Completed

### Fix 1: 登录页语言切换
`LoginPage.tsx` 右上角新增 Select 语言切换组件，复用 `useLanguageStore` + `LANGUAGES` 常量，与 Navbar 保持一致。

### Fix 2: 高风险学生导航不高亮
**根因**：Sidebar `selectedKey` 遍历 `allKeys` 时，`/dashboard` 比 `/dashboard/at-risk` 更早匹配（startsWith 前缀碰撞）。  
**修复**：将 `allKeys` 按字符串长度降序排序，最长路径优先匹配，保证精确路由高亮。

### Fix 3: principal 看不到 KPI 面板
`ROLE_HOME.principal` 从 `/dashboard/at-risk` 改为 `/dashboard`，登录后直接进入带 KPI 卡的全局概览页。

### Fix 4: 教师仪表盘内容补全
**后端**：`/dashboard/stats` teacher 分支新增：
- `pendingGrading`：teacher 课程下还没有任何成绩的 gradeItem 数量
- `attendanceAlerts`：teacher 课程下出勤率 < 75% 的学生列表（最多5条）

**前端**：
- 4 个 KPI 卡（我的课程 / 我的学生 / 待批改 / 出勤异常数）
- 「出勤异常提醒」列表替换「最近成绩」，显示学生姓名 + 出勤率进度条

### Fix 5: 学生仪表盘补全
**后端**：student 分支新增：
- `courseSchedules`：已修课程名称 + 排课时间（来自 CourseAssignment.schedule）
- `attendanceBreakdown`：{present, absent, late, excused} 四维分解

**前端**：
- 「我的课表」Card：列出所有选课及时间安排
- 「考勤详情」Card：4 种状态横向进度条 + 总节数

### Fix 6: 管理员仪表盘图表
- 柱状图：去掉竖向 grid lines 和轴线，添加顶部数值 label，圆角增大至 6px
- 出勤趋势：从静态圆形指示器改为 8 周 AreaChart（渐变填充），后端新增 `weeklyAttendance` 数组

### i18n
三语言新增 key：`pendingGrading / attendanceAlerts / noAlerts / myTimetable / attendanceDetail / noSchedule / attendanceTrendWeekly`

## Files Changed
- `pc/src/pages/auth/LoginPage.tsx` — 语言切换 Select
- `pc/src/layouts/Sidebar.tsx` — selectedKey 修复 + ROLE_HOME.principal
- `pc/src/pages/dashboard/DashboardPage.tsx` — 教师 + 管理员仪表盘增强
- `pc/src/pages/dashboard/StudentDashboardPage.tsx` — 课表 + 考勤详情
- `pc/src/types/index.ts` — 新增 CourseSchedule / AttendanceBreakdown / AttendanceAlert
- `pc/src/locales/en.ts`, `zh.ts`, `ms.ts` — 新 key
- `backend/src/routes/dashboard.ts` — teacher/student/admin stats 增强
