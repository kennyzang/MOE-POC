# Dev Log 260525-1910 — Stage E: SIS 录取 4 步向导

## 摘要（Human Review）

实现 SIS 录取模块 4 步 Wizard，含年龄-年级校验、Review 弹窗状态流转、后端新建/状态接口。Playwright 全流程验证通过，TypeScript 零错误，双仓库推送成功。

## 对话信息

- 开始时间：2026-05-25 19:10
- 结束时间：2026-05-25 19:30
- Token 估算：~60k

## 完成内容

### Stage E：SIS 录取 4 步向导

**后端（`backend/src/routes/admissions.ts`）**
- 新增 `POST /api/v1/admissions`：创建录取申请，校验必填字段
- 新增 `PATCH /api/v1/admissions/:id/status`：专用状态更新接口，有效状态流转校验（pending→under_review→accepted|rejected），已决定时自动写入 `decidedAt`
- 注意：`PATCH /:id/status` 必须注册在 `PATCH /:id` 之前，否则被通配路由拦截

**前端（`pc/src/pages/sis/AdmissionsPage.tsx`）**
- 列表页右上角添加「+ New Application」按钮
- 4 步 Wizard Modal（720px，destroyOnHidden）
  - Step 1 Applicant Info：姓名(必填)、IC号、出生日期(必填)、性别(必填)、国籍(默认Bruneian)
  - Step 2 Academic Info：年级(必填)、前就读学校；年龄-年级自动校验，antd Alert type=warning 提示但不阻止提交
  - Step 3 Parent Info：家长姓名(必填)、电话(必填)、邮箱(email校验)、关系(Select)
  - Step 4 Review & Submit：三组 Descriptions 展示所有信息 + ADM-2026-XXX 参考号 + Submit 按钮
- 每步独立 Form，validateFields() 后才能 Next
- 提交：POST /api/v1/admissions，成功后关闭 Modal、刷新列表
- Review Modal 改用 `PATCH /api/v1/admissions/:id/status`（原来是 PATCH /:id）

**i18n（en/zh/ms 三语言）**
- `admissions` 命名空间新增：`wizard.*`（step1-4、ageMismatch、applicationId、submitSuccess）、`newApplication`、`statusChange`、`markUnderReview`、`icNumber`、`dateOfBirth`、`nationality`、`relationship*`
- `common` 命名空间新增：`previous`、`required`、`invalidEmail`

**类型（`pc/src/types/index.ts`）**
- `Admission` 接口添加 `icNumber`、`parentRelationship` 字段

### 验证结果（Playwright）

- 列表页显示「+ New Application」按钮 ✓
- Wizard 4 步进度条正常 ✓
- Step 2 选择 Year 11（申请人14岁）→ Alert 警告「Age-Grade Mismatch: The applicant age (14) may not match Year 11. Standard age range is 15–17.」✓
- Step 4 展示 Application Reference ADM-2026-478 和所有已填信息 ✓
- Submit 后 Wizard 关闭，列表 Total 5→6，Pending Review 3→4，「Ahmad Bin Khalid」出现在列表第一行 ✓
- TypeScript `npx tsc --noEmit` 前后端均零错误 ✓

## 修改文件列表

- `backend/src/routes/admissions.ts`（+106 行）
- `pc/src/locales/en.ts`（+22 行）
- `pc/src/locales/zh.ts`（+22 行）
- `pc/src/locales/ms.ts`（+22 行）
- `pc/src/pages/sis/AdmissionsPage.tsx`（404→585 行）
- `pc/src/types/index.ts`（+2 行）

## Git

- Commit：`6df1fc0` feat(sis): SIS 录取 4 步向导 Admission Wizard (Stage E)
- Push：origin master ✓，github master ✓
