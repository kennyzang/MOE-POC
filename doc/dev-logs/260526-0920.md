# Dev Log 260526-0920 — SMTP 系统配置页面 + 密钥泄露修复

## 摘要（人工阅读）

两件事：1) 修复 SMTP 密码意外提交到 GitHub 的安全问题（git history rewrite + force push）；2) 实现系统设置页面，管理员可在界面配置 SMTP，无需修改 .env。

## 会话信息

- 时间：2026-05-26 08:54 — 09:20
- Token 估算：~20,000

## 完成工作

### 安全修复

- 发现计划文档 `docs/superpowers/plans/2026-05-26-notification-system.md` 包含明文 SMTP 密码，已被 GitGuardian 检测
- 用 `git filter-branch` 重写历史（9 个 commit），彻底清除密码
- GitHub force push 成功；GitLab 因分支保护需人工操作
- 计划文档密码替换为 `<your-smtp-password>` 占位符

### SMTP 系统配置

**后端新增：**
- `backend/src/routes/config.ts` — `GET/PUT /api/v1/config/smtp` + `POST /api/v1/config/smtp/test`
  - admin 专属路由
  - 密码字段响应时自动脱敏（返回 `••••••••`）
  - 保存时跳过 `••••••••`（留空保留已有密码）
  - 测试发邮件：发送到指定地址或当前用户邮箱

- `backend/src/services/emailService.ts` — 改为动态读取 SMTP 配置
  - 优先读 `SystemConfig` 表（DB）
  - DB 未配置则 fallback 到 `.env`
  - 每次发邮件时动态创建 transporter（支持热更新）

**前端新增：**
- `pc/src/pages/settings/SettingsPage.tsx` — SMTP 配置表单
  - SMTP Host / Port / Username / Password / From Address
  - 密码字段：留空保留现有，输入新值则覆盖
  - "Save Configuration" 按钮
  - "Send Test Email" 区域（输入收件人地址，点击发送）
  - 三语言支持

**路由更新：**
- `pc/src/router/routes.tsx` — `/settings` 从占位页改为真实 `SettingsPage`

## 文件变更列表

### 新增
- `backend/src/routes/config.ts`
- `pc/src/pages/settings/SettingsPage.tsx`

### 修改
- `backend/src/services/emailService.ts`（动态读 DB + .env fallback）
- `backend/src/index.ts`（注册 /api/v1/config）
- `pc/src/router/routes.tsx`
- `pc/src/locales/en.ts / zh.ts / ms.ts`（新增 settings.* 键）
- `docs/superpowers/plans/2026-05-26-notification-system.md`（密码脱敏）

## 验证结果

- `backend npx tsc --noEmit` — ✅ 0 errors
- `frontend npx tsc --noEmit` — ✅ 0 errors
- Playwright 截图 — ✅ 设置页面完整显示（Host/Port/User/Password/From/保存/测试）
- git history 扫描 `qwrrmlnjmwmbcfsm` — ✅ 0 条匹配
- GitHub force push — ✅ 成功
