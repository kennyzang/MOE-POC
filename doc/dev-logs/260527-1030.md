# Dev Log 260527-1030

## Summary (for human review)
修复两个人工验收发现的问题：登录页快捷登录失败 + 高风险学生面板多语言不彻底/无排名。

## Session Info
- Date: 2026-05-27
- Time: ~10:30
- Token estimate: ~12k
- Commits: 1 (`5758492`)

## Work Completed

### Fix 1: 登录页快捷登录密码问题
**问题**：快捷登录对所有账号硬编码 `Demo@2026`，导致 admin（admin123）、finance（finance123）快捷登录失败；缺少 principal 和 hod01 按钮。

**修复**：
- `DEMO_ACCOUNTS` 每条记录新增 `password` 字段，各账号密码独立
- `onQuickLogin` 函数签名改为接收 `password` 参数
- 新增 Principal（principal123）和 HOD（hod123）两个快捷登录按钮
- 现有 8 个角色全部覆盖

### Fix 2: 高风险学生面板 i18n + 排名
**问题**：页面大量硬编码英文字符串，切换中文/马来语时不翻译；无排名列。

**修复**：
- 三个 locale 文件（en/zh/ms）新增 `atRisk.*` 命名空间（25 个 key）
- `AtRiskPage.tsx` 所有硬编码字符串替换为 `t()` 调用：页头副标题、刷新按钮、加载提示、空状态、统计卡片标签、表格列头、Tooltip、状态标签、底部图例
- `RiskBadge` 和 `StudentChart` 子组件内部也调用 `useTranslation`
- 新增 Rank 列：按 `riskScore` 降序预排，#1 为最高风险学生（红色高亮）

## Files Changed
- `pc/src/pages/auth/LoginPage.tsx` — 快捷登录密码独立 + 新增 Principal/HOD 按钮
- `pc/src/pages/dashboard/AtRiskPage.tsx` — 全页面 i18n + Rank 列
- `pc/src/locales/en.ts` — 新增 atRisk.* namespace（25 keys）
- `pc/src/locales/zh.ts` — 同上（中文翻译）
- `pc/src/locales/ms.ts` — 同上（马来语翻译）
