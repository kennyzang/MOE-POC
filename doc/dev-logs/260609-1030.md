# Dev Log 260609-1030

## Summary
修复部署后接口仍请求 localhost 的问题。全面排查 PC 前端所有 hardcoded localhost:4000 fallback，共涉及 10 个文件。

## 时间
2026-06-09 ~10:30（约 15 分钟）

## Token 估算
~8k tokens

## 完成工作

### 问题排查
- 截图显示 `/api/v1/registration/schools` 请求打到 `http://localhost:4000`
- 根因：`VITE_API_URL` 未在生产构建中配置，代码 fallback 直接写死了 localhost

### 修复内容（10 个文件）

**类型 A — EventSource SSE 流（7 个页面）**  
将 fallback 从 `'http://localhost:4000/api/v1'` 改为 `'/api/v1'`（相对路径）：
- `pages/dashboard/CommandCenterPage.tsx`
- `pages/dashboard/StudentDashboardPage.tsx`
- `pages/dashboard/ParentAttendancePage.tsx`
- `pages/dashboard/ParentChildrenPage.tsx`
- `pages/dashboard/ParentGradesPage.tsx`
- `pages/finance/FinanceDashboardPage.tsx`
- `pages/counselor/CounselorDashboardPage.tsx`
- `pages/settings/ThresholdsPage.tsx`

**类型 B — Registration 页（2 个页面）**  
替换为共享 `api` 实例（`@/lib/api`，baseURL 已为 `/api/v1`）：
- `pages/registration/RegistrationPortalPage.tsx`：移除 `axios` import + `API` 常量，改用 `api.get/post`
- `pages/registration/RegistrationStatusPage.tsx`：同上

## 验证
- `npm run build` 通过，零 TS 错误
- `grep localhost:4000 pc/src/**` 无结果

## Commits
- `fix(api): remove hardcoded localhost:4000 fallbacks from all frontend pages`
