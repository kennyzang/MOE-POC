# Dev Log — 260605-1800

## Summary
Redesigned login page Quick Access section to match UNISSA demo style.

## Work Done

### Login Page — Demo Accounts Panel (`pc/src/pages/auth/LoginPage.tsx`)
- Removed the colored role-button grid ("Quick Access") from inside the login card
- Added a `DemoAccountsPanel` component that floats to the **right** of the login card
- Panel shows all 17 demo accounts in a table with Role / Username / Password (masked) columns
- Clicking a row **fills** the form fields (username + password) without auto-logging in
- Authority badges (MOE / MORA / PVT) shown inline in the Role column
- Panel is collapsible via a `ChevronRight` button; when closed a "Show Demo Accounts" dashed button appears at the bottom of the card to reopen it
- Used `Form.useForm()` + `form.setFieldsValue()` to wire up the fill behavior

## Files Changed
- `pc/src/pages/auth/LoginPage.tsx`

## Tokens Estimate
~3k input / ~4k output
